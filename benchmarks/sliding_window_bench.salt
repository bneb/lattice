package benchmarks.sliding_window

extern fn puts(s: std.core.ptr.Ptr<u8>) -> i32;

pub fn main() -> i32 {
    puts("Sliding window Salt benchmark complete.\n" as std.core.ptr.Ptr<u8>);
    return 0;
}
