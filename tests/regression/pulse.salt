// tests/regression/pulse.salt
// Regression Test: Pulse Attribute Yield Check
// Ensures that @yielding functions emit salt_yield_check in loops.

@yielding(1024)
pub fn heavy_work() {
    let mut i = 0;
    while i < 10000 {
        i = i + 1;
    }
}

// CHECK: func.call @__salt_yield_check

pub fn main() {}
