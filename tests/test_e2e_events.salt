package tests.test_e2e_events

// Epic 50: Event Routing Matrix E2E Test
// All execution happens in the C-bridge (event_bridge.c) to avoid MLIR import conflicts.
extern fn event_routing_e2e_test();

pub fn main() -> i32 {
    println("--- Epic 50: Event Routing Matrix E2E Test ---");
    event_routing_e2e_test();
    return 0;
}
