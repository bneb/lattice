[package]
name = "rust_selectors"
version = "0.1.0"
edition = "2021"

[dependencies]
scraper = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
